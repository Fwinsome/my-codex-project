#import "template.typ": *
#import "@preview/subpar:0.2.2"

#let today = datetime.today().display("[year]年[month]月[day]日")

#let my_config = (
  学号: "3240104295",
  姓名: "李宇谦",
  专业: "电子信息工程",
  日期: today,
  地点: "东3-406",
  课程名称: "电路与电子技术实验",
  实验名称: "基本比较器",
  指导老师: "傅晓程",
  实验类型: "",
  成绩: "",
  同组学生姓名: "",
)

#let photo(path) = image(path, width: 92%)

#show: zjuLabReport.with(config: my_config)

#set heading(numbering: none)
#set par(justify: true)

= 一：实验目的和要求
本实验围绕电压比较器及其典型应用展开，要求在理解器件结构与工作机理的基础上，完成过零比较器、单门限比较器、滞回比较器、窗口比较器和三态比较器等电路的搭建、测试与分析。实验中既要关注比较器的阈值、电压传输特性与动态响应，也要结合器件输出结构与应用场景理解不同方案的适用性。

1. 了解电压比较器与运算放大器在结构设计目标和工作方式上的差异。
2. 掌握电压比较器的基本传输特性、阈值电压和响应时间等概念。
3. 学习利用示波器时域模式和 XY 模式测量输出波形及电压传输特性曲线。
4. 理解比较器在 PWM 产生、抗干扰门限判决、窗口检测和多状态判别中的工程应用。

= 二：实验内容和原理
电压比较器用于比较两个模拟电压的大小关系，并将比较结果转换为高、低两种稳定输出电平。与运算放大器不同，比较器主要面向开环或带正反馈的非线性工作状态设计，强调快速翻转能力、明确的阈值判决和较强的接口适配能力；而运算放大器则主要服务于负反馈条件下的线性放大，对稳定性、带宽与压摆率的设计侧重点不同。

本次实验主要涉及以下五类电路。

1. 过零电压比较器：以 0 V 为判决基准，当输入正弦波越过零点时输出翻转，可实现正弦波到方波的变换，并可由此测量输出边沿时间与传输特性。
2. 单门限电压比较器：以可调直流电压作为参考门限，将三角波与门限比较，输出方波占空比随门限改变而变化，从而实现 PWM 产生。
3. 滞回电压比较器：在比较器中引入正反馈，形成上、下两个不同门限电压，获得迟滞特性，从而提高抗干扰能力并抑制门限附近抖动。
4. 窗口电压比较器：设置上、下两个独立参考电平，对输入电压是否落在指定窗口范围内进行判别。
5. 三态电压比较器：通过双门限和二极管网络得到三个稳定输出区间，用于表示输入位于低区、中间区或高区。

从器件特性看，LM393 为集电极开路输出结构，实验中需要通过上拉电阻建立高电平；LM358 为运算放大器型器件，其输出不需要外接上拉电阻，但由于压摆率有限，在比较器应用中会表现出更缓慢的翻转过程和更明显的传输斜率。

= 三：主要仪器设备
LM393 双电压比较器、LM358 集成运算放大器、双踪数字示波器及配套探头、函数信号发生器、直流稳压电源、面包板、电阻、电位器及导线等基础实验器材。

= 四：操作方法和实验步骤
1. 按实验要求分别搭建 LM393 过零比较器、LM358 过零比较器、LM393 单门限比较器、反相滞回比较器、窗口比较器和三态比较器电路，并检查电源极性、接地和上拉电阻连接是否正确。
2. 向比较器输入规定频率与幅值的正弦波或三角波信号，先在时域模式下观察输入与输出波形，再调整示波器量程以获得清晰的翻转过程。
3. 对过零比较器测量输出高、低电平及上升时间、下降时间；对单门限比较器逐点改变参考电压，记录对应输出占空比；对滞回、窗口和三态比较器记录阈值与输出状态变化。
4. 将示波器切换至 XY 模式，以输入电压作为 X 轴、输出电压作为 Y 轴，观察和记录电压传输特性曲线，判断电路是否满足理论门限关系。
5. 对实验数据进行整理与比较，结合器件输出结构、压摆率和正反馈机理分析不同电路的工作特点与误差来源。

= 五：实验数据记录和处理
== （一）过零电压比较器
LM393 与 LM358 过零比较器均采用 1 kHz、1 V 正弦波作为输入。由示波器测得的典型动态参数如表 1 所示。

#figure(
  table(
    columns: 5,
    align: center + horizon,
    stroke: none,
    inset: 4pt,
    toprule(),
    [器件], [输出峰-峰值], [上升时间], [下降时间], [说明],
    midrule(),
    [LM393], [25.3 V], [610 ns], [260 ns], [集电极开路输出，实验中接上拉电阻],
    [LM358], [23.17 V], [50.5 us], [54.9 us], [无外接上拉电阻，转换过程较缓],
    bottomrule(),
  ),
  caption: [过零电压比较器典型测量结果],
)

#subpar.grid(
  figure(photo("figures/bw/exp1_1.jpg"), caption: [LM393 输入与输出波形]), <exp1_lm393_wave>,
  figure(photo("figures/bw/exp1_2.jpg"), caption: [LM393 电压传输特性]), <exp1_lm393_xy>,
  figure(photo("figures/bw/exp1_3.jpg"), caption: [LM393 上升沿放大测量]), <exp1_lm393_edge>,
  columns: (1fr, 1fr, 1fr),
  caption: [LM393 过零电压比较器实验结果],
)

#subpar.grid(
  figure(photo("figures/bw/exp1_4.jpg"), caption: [LM358 输入与输出波形]), <exp1_lm358_wave>,
  figure(photo("figures/bw/exp1_5.jpg"), caption: [LM358 电压传输特性]), <exp1_lm358_xy>,
  figure(photo("figures/bw/exp1_6.jpg"), caption: [LM358 传输区放大波形]), <exp1_lm358_edge>,
  columns: (1fr, 1fr, 1fr),
  caption: [LM358 过零电压比较器实验结果],
)

#figure(
  photo("figures/exp1_sim.jpg"),
  caption: [过零电压比较器仿真结果],
)

== （二）单门限电压比较器
单门限比较器实验中，以可调直流电压 $U_in$ 作为比较门限，以 1 kHz 三角波作为被比较信号。根据给定实验数据得到输出占空比记录，如表 2 所示。

#figure(
  table(
    columns: 2,
    align: center + horizon,
    stroke: none,
    inset: 4pt,
    toprule(),
    [$U_in$ / V], [输出占空比 / %],
    midrule(),
    [0.0], [50.5],
    [0.2], [48.3],
    [0.8], [42.3],
    [1.7], [33.4],
    [2.2], [28.4],
    [2.6], [24.4],
    [3.3], [17.5],
    [4.0], [10.57],
    [4.5], [5.6],
    [4.9], [1.6],
    [5.0], [0.6],
    bottomrule(),
  ),
  caption: [单门限比较器输入电压与输出占空比数据],
)

#figure(
  image("figures/exp2_plot.svg", width: 88%),
  caption: [单门限比较器输入电压与输出占空比关系曲线],
)

#figure(
  photo("figures/exp2_sim.jpg"),
  caption: [单门限电压比较器仿真结果],
)

== （三）滞回电压比较器
滞回比较器实验要求回差电压约为 0.5 V。图 4(b) 为未调整前的回转电压传输特性，对应电阻比为 5.1 kΩ : 200 kΩ，光标读得迟滞宽度约为 0.5825 V；图 4(c) 为调整后的传输特性，对应电阻比为 4.18 kΩ : 200 kΩ，迟滞宽度约为 0.5000 V，更接近设计目标。

#subpar.grid(
  figure(photo("figures/bw/exp3_1.jpg"), caption: [输入与输出波形]), <exp3_wave>,
  figure(photo("figures/bw/exp3_2.jpg"), caption: [未调整前回转电压传输特性（5.1 kΩ : 200 kΩ）]), <exp3_xy1>,
  figure(photo("figures/bw/exp3_3.jpg"), caption: [调整后回转电压传输特性（4.18 kΩ : 200 kΩ）]), <exp3_xy2>,
  columns: (1fr, 1fr, 1fr),
  caption: [滞回电压比较器实验结果],
)

#subpar.grid(
  figure(photo("figures/exp3_1_sim.jpg"), caption: [滞回电压比较器仿真传输特性（输入由 -3 V 扫描至 3 V）]), <exp3_sim1>,
  figure(photo("figures/exp3_2_sim.jpg"), caption: [滞回电压比较器仿真传输特性（输入由 3 V 扫描至 -3 V）]), <exp3_sim2>,
  columns: (1fr, 1fr),
  caption: [滞回电压比较器仿真结果],
)

== （四）窗口电压比较器
窗口比较器实验中，参考电压设置为 `V_ref1 = 1.0 V`、`V_ref2 = 4.0 V`，因而窗口宽度为 3.0 V。示波器测得当输入三角波落入相应窗口区间时，输出维持稳定电平；离开窗口后输出发生翻转。

#subpar.grid(
  figure(photo("figures/bw/exp4_1.jpg"), caption: [输入输出波形及门限光标]), <exp4_wave>,
  figure(photo("figures/bw/exp4_2.jpg"), caption: [窗口比较器传输特性]), <exp4_xy>,
  columns: (1fr, 1fr),
  caption: [窗口电压比较器实验结果],
)

== （五）三态电压比较器
三态比较器实验中，输入为 1 kHz 三角波，理论参考门限为 `V_ref1 = -2.5 V`、`V_ref2 = +2.5 V`。实验结果显示输出能够在低电平、零电平与高电平三种状态之间切换，形成分段判别特性。

#subpar.grid(
  figure(photo("figures/bw/exp5_1.jpg"), caption: [三态输出时域波形]), <exp5_wave>,
  figure(photo("figures/bw/exp5_2.jpg"), caption: [三态传输特性]), <exp5_xy>,
  columns: (1fr, 1fr),
  caption: [三态电压比较器实验结果],
)

#subpar.grid(
  figure(photo("figures/exp5_1_sim.jpg"), caption: [三态电压比较器仿真结果（未并联电容，存在由二极管结电容引起的毛刺）]), <exp5_sim1>,
  figure(photo("figures/exp5_2_sim.jpg"), caption: [三态电压比较器仿真结果（并联电容后，毛刺得到抑制）]), <exp5_sim2>,
  columns: (1fr, 1fr),
  caption: [三态电压比较器仿真结果],
)

= 六：实验结果与分析
== （一）过零电压比较器
LM393 过零比较器的输出翻转陡峭，图 1 中可见输入正弦波越过零点后输出迅速进入高、低饱和状态，XY 模式下电压传输特性近似竖直，表明其作为专用比较器具有较强的门限判决能力。实测上升时间约为 610 ns、下降时间约为 260 ns，说明器件在开关工作条件下具备较快的动态响应。

与之相比，LM358 过零比较器虽然同样实现了零点判决，但由于其本质上仍是运算放大器，输出边沿明显变缓，图 2 中的传输特性呈现斜坡状而非理想竖直跳变，实测上升、下降时间均在 50 us 量级。查阅器件典型压摆率约为 0.3 V/us，可估算二十余伏输出摆幅对应的翻转时间应在几十微秒量级，与实验结果同量级，说明 LM358 的动态过程已主要受压摆率限制。由此可见，LM358 在要求不高时可充当比较器，但其动态性能明显逊于专用比较器 LM393。

== （二）单门限电压比较器
表 2 与图 3 表明，随着输入门限电压 $U_in$ 增大，输出高电平占空比单调下降，且整体近似线性。若将输入三角波视为峰值约为 ±5 V 的对称波形，则高电平持续时间与三角波高于门限的区间长度成正比，因此占空比理论上满足近似线性关系，这与实验数据吻合较好。

当 $U_in = 0$ V 时，占空比约为 50.5%，说明门限位于三角波中值附近；当 $U_in$ 逐渐增大到 5 V 时，高电平导通区迅速缩短，占空比降至 0.6%。这说明单门限比较器能够方便地把模拟门限变化转换为 PWM 波形宽度变化，是模拟量控制数字占空比的典型实现方式。

== （三）滞回电压比较器
滞回比较器最显著的特点是上、下翻转门限不同。由图 4 可见，输入信号上升与下降时沿不同门限触发输出翻转，传输特性形成明显闭合回线，而不再是单一的竖直线。未调整前电阻比为 5.1 kΩ : 200 kΩ 时，实测迟滞宽度约为 0.5825 V；将电阻比调整为 4.18 kΩ : 200 kΩ 后，迟滞宽度收敛到约 0.5000 V，与设计要求更加一致。

由于门限分裂为两个值，输入信号在门限附近的小幅波动不会立刻引起输出反复翻转，因此电路抗干扰能力明显优于单门限比较器。该实验也表明，正反馈是产生迟滞特性的关键，当反馈网络参数确定后，迟滞宽度可由电阻比进行调节。

从仿真结果看，当输入分别按 `-3 V -> 3 V` 和 `3 V -> -3 V` 两个方向扫描时，所得传输曲线整体形状基本一致，但会因上、下翻转门限不同而在坐标轴上发生平移，这正是滞回特性的直观体现。

== （四）窗口电压比较器
窗口比较器相当于把两个门限判决组合在一起。图 5 中光标给出的门限分别为 1.0 V 和 4.0 V，对应窗口宽度 3.0 V；当输入三角波位于窗口范围内时，输出保持一种稳定状态，超出窗口后则翻转到另一稳定状态。XY 模式下的矩形回线进一步说明电路能够对输入所在区间进行清晰分段。

该类电路常用于幅值筛选、门限区间检测和参数分档等场合。与单门限比较器相比，窗口比较器对输入范围的判别更精细，能够直接给出“在窗口内”或“在窗口外”的逻辑结果。

== （五）三态电压比较器
三态比较器在两个门限的基础上进一步把输出划分为三种状态：当输入低于下门限时输出为低电平，当输入位于两门限之间时输出近似为 0 V，当输入超过上门限时输出为高电平。图 6 中的时域波形与 XY 传输特性均呈现明显的三段结构，说明三态判决功能已经实现。

同时，图 6 的时域波形在跃迁瞬间出现了较明显的尖峰毛刺，这与 PPT 中给出的现象一致，主要来源于二极管结电容和实际连线寄生参数引起的瞬态耦合。若在实际工程中需要进一步抑制该类毛刺，可通过优化器件选型、减小布线寄生参数和增加适当的缓冲或滤波环节加以改善。

从两张仿真图的对比也可以看到，在相关电阻两端未并联电容时，输出跃迁附近存在较明显毛刺；并联适当电容后，二极管结电容带来的瞬态尖峰被明显削弱，输出波形更加平滑，这说明附加电容能够起到一定的吸收和缓冲作用。

= 七：思考与讨论
1. 电压比较器的作用与特点是什么？
电压比较器的作用是比较两个输入电压的大小，并把结果转换为高、低或多状态输出，常用于门限检测、波形变换和接口转换。它工作在开环或带正反馈的非线性区，特点是翻转快、阈值明确、输出状态稳定，但不适合承担线性放大任务。

2. 集成运算放大器能否作为电压比较器使用？集成电压比较器能否作为运算放大器使用？为什么？
集成运算放大器在速度要求不高时可以临时作为比较器使用，但其压摆率和恢复过程通常较慢；集成电压比较器一般不能代替运算放大器，因为比较器多数按开环判决优化，往往缺少线性放大所需的频率补偿，接入负反馈后容易不稳定。

3. 电压比较器电路是否需要调零？原因何在？
电压比较器电路通常不以零漂最小为主要目标，因此一般不专门调零。它更关注门限位置、响应时间和抗干扰能力。只有在高精度阈值比较场合，输入失调电压会直接造成门限偏移，这时才需要通过补偿、校准或选用低失调器件减小误差。

4. 电压比较器电路两个输入电阻是否要求对称？为什么？
比较器两个输入电阻并不一定必须完全对称，但若两端等效电阻差别过大，输入偏置电流会在电阻上形成不同压降，从而带来附加门限误差。因此在高精度设计中常尽量让两输入端看到相近电阻，或在计算门限时把偏置电流影响一并考虑进去。

5. 电压比较器输出电压由什么决定？
电压比较器的输出电压首先由电源范围和输出级结构决定。对于推挽输出，高、低电平通常受器件供电电压限制；对于 LM393 这类开集电极输出，高电平还取决于上拉电阻连接到的外部电源，低电平则由内部晶体管导通能力和负载条件共同决定。
